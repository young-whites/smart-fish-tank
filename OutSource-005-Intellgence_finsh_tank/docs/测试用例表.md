# 智能鱼缸系统 — 测试用例表

> 版本：V1.0 | 日期：2026-05-03
>
> 基于当前代码实现的功能模块编写，覆盖 11 个源文件、7 个业务线程。

---

## 一、模块清单与实现状态

| 模块 | 文件 | 线程 | 优先级 | 状态 |
|------|------|------|--------|------|
| 传感器采集 | app_sensor.c/h | t_sens | 15 | ✅ 已实现 |
| 自动控制 | app_control.c/h | t_ctrl | 12 | ✅ 已实现 |
| 声光报警 | app_alarm.c/h | t_alarm | 13 | ✅ 已实现 |
| 喂食控制 | app_feed.c/h | t_feed | 16 | ✅ 已实现 |
| OLED 显示 | app_display.c/h + drv_oled.c/h | t_disp | 18 | ✅ 已实现 |
| 按键扫描 | app_key.c/h | t_key | 14 | ✅ 已实现 |
| WiFi 通信 | app_wifi.c/h | t_wifi | 17 | ✅ 已实现 |
| 共享数据 | app_data.c/h | — | — | ✅ 已实现 |
| 汉字字库 | font_lib.c/h | — | — | ✅ 已实现 |
| OLED 驱动底层 | drv_oled.c/h | — | — | ✅ 已实现 |
| 系统入口 | main.c | main | 10 | ✅ 已实现 |

---

## 二、测试用例表

### 2.1 传感器采集模块 (app_sensor)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-SEN-001 | DS18B20 温度读取 | PB13 接 DS18B20，上电运行 | 等待 2s 采集周期 | `g_sensor.water_temp` 更新为实际水温值，精度 ±0.5°C | FinSH: `sensor` 查看 |
| TC-SEN-002 | DS18B20 断线容错 | 拔掉 DS18B20 传感器 | 等待 3s | `water_temp` 保持上一次有效值，不更新为异常值（-999） | FinSH: `sensor` |
| TC-SEN-003 | 空气质量 ADC 采集 | PA4 接 MQ-135 AO | 等待 500ms | `g_sensor.air_quality` 更新为 0~4095 范围内的 ADC 原始值 | FinSH: `sensor` |
| TC-SEN-004 | 水位 ADC 采集 | PA1 接水位传感器 AO | 等待 500ms | `g_sensor.water_level` 更新为 0~100% 百分比值 | FinSH: `sensor` |
| TC-SEN-005 | PH 值 ADC 采集 | PA0 接 PH 传感器 AO | 等待 500ms | `g_sensor.ph_value` 更新为 0.0~14.0 范围内浮点值 | FinSH: `sensor` |
| TC-SEN-006 | 采集周期验证 | 系统正常运行 | 用示波器/逻辑分析仪观察 PB13 | DS18B20 每次转换间隔约 750ms+500ms=1.25s | 示波器测量 |
| TC-SEN-007 | 多通道 ADC 切换 | PA0/PA1/PA4 均接信号 | 等待 1s | 三个通道数据互不干扰，各自独立更新 | FinSH: `sensor` |

### 2.2 自动控制模块 (app_control)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-CTL-001 | 低温自动加热 | 模式=自动，水温 < temp_lower | 手动冷却传感器或调低阈值 | PB12 继电器吸合（加热开启） | 万用表测 PB12 电平 |
| TC-CTL-002 | 高温停止加热 | 水温 > temp_upper | 加热进行中，水温上升 | PB12 继电器释放（加热关闭） | 万用表测 PB12 |
| TC-CTL-003 | 水位高自动排水 | 水位 > water_level_max | 注水使水位超标 | PB15 吸合（排水），PB14 释放 | 测 PB15/PB14 电平 |
| TC-CTL-004 | 水位低自动加水 | 水位 < water_level_min | 水位降低 | PB14 吸合（加水），PB15 释放 | 测 PB14/PB15 电平 |
| TC-CTL-005 | 水位正常区间 | 水位在 min~max 之间 | 正常水位 | PB14 和 PB15 均释放 | 测电平 |
| TC-CTL-006 | 空气超标开增氧 | air_quality > air_max | MQ-135 检测到超标 | PA15 吸合（增氧开启） | 测 PA15 电平 |
| TC-CTL-007 | 空气恢复关增氧 | air_quality ≤ air_max | 空气恢复正常 | PA15 释放 | 测 PA15 |
| TC-CTL-008 | 手动模式不自动控 | 模式=手动 | 等待 2s | 所有继电器不随传感器自动动作 | 观察继电器状态 |
| TC-CTL-009 | 模式切换: 自动→手动 | 正在自动运行 | KEY3 切换模式 | 所有继电器关闭，进入手动状态 | 继电器全部释放 |
| TC-CTL-010 | 控制线程周期 | 系统运行 | 观察继电器动作延迟 | 最大响应时间 ≤ 500ms（线程周期） | 示波器测量 |

### 2.3 声光报警模块 (app_alarm)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-ALR-001 | 温度越限报警 | alarm_enable=1，水温超出阈值 | 使水温超标 | PB9 蜂鸣器响 0.5s / 停 2s 循环 | 听觉 + 测 PB9 |
| TC-ALR-002 | 水位异常报警 | 水位超出 min/max | 使水位超标 | 蜂鸣器报警（同上模式） | 听觉 |
| TC-ALR-003 | 空气超标报警 | air > air_max | MQ-135 检测超标 | 蜂鸣器报警 | 听觉 |
| TC-ALR-004 | PH 越限报警 | PH 超出 lower/upper | 使 PH 超标 | 蜂鸣器报警 | 听觉 |
| TC-ALR-005 | 报警自动解除 | 正在报警中 | 恢复参数到正常范围 | 蜂鸣器停止 | 听觉 |
| TC-ALR-006 | 报警关闭 | alarm_enable=0，参数超标 | 不触发任何操作 | 蜂鸣器不响 | 听觉 |
| TC-ALR-007 | 多参数同时越限 | 温度+水位同时超标 | 同时超限 | 蜂鸣器报警（不叠加，统一模式） | 听觉 |
| TC-ALR-008 | 邮箱事件传递 | alarm_check 触发 | 观察邮箱 mb_alarm | 报警事件通过邮箱传递给 t_alarm 线程 | 代码逻辑验证 |

### 2.4 喂食控制模块 (app_feed)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-FED-001 | 自动定时喂食 | feed_interval 设置，倒计时运行 | 等待倒计时归零 | 舵机打开 3s 后关闭，倒计时重置 | 观察舵机动作 |
| TC-FED-002 | 倒计时显示 | 倒计时 > 0 | 查看 OLED 主页面子页 2 | 喂食行显示 MM:SS 格式倒计时 | OLED 显示 |
| TC-FED-003 | 倒计时归零显示 | 倒计时 = 0 | 查看 OLED | 喂食行显示 `--:--` | OLED 显示 |
| TC-FED-004 | 手动触发喂食 | 系统运行 | 调用 `app_feed_trigger()` | 舵机立即打开 3s，倒计时重置 | 观察舵机 |
| TC-FED-005 | 舵机 PWM 输出 | TIM3/PA6 接舵机 | 触发喂食 | PA6 输出 50Hz PWM，CCR1=2500（开）/1500（关） | 示波器测 PA6 |
| TC-FED-006 | 喂食状态标志 | 喂食中 | 观察 `g_status.feeding` | 喂食期间 =1，结束后 =0 | FinSH 或代码调试 |
| TC-FED-007 | 喂食间隔可配置 | 修改 feed_interval | 设置为 7200（2h） | 倒计时从 7200 开始递减 | OLED 显示验证 |

### 2.5 OLED 显示模块 (app_display + drv_oled)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-DIS-001 | 主页面子页1显示 | 上电默认 | 上电等待 1s | 显示：温度、空气、水位、PH值，底部 P1/2 | OLED 目视 |
| TC-DIS-002 | 主页面子页2翻页 | 主页面 P1 | 按 KEY2 | 显示：模式、喂食倒计时、WiFi、热氧状态，底部 P2/2 | OLED 目视 |
| TC-DIS-003 | 主页面→阈值设置 | 主页面任意子页 | 按 KEY1 | 进入阈值设置页（P1），显示温下限/温上限/空气值 | OLED 目视 |
| TC-DIS-004 | 阈值设置子页2翻页 | 阈值页 P1 | 按 KEY2 | 显示 PH 下限/上限、水位低/高，底部 P2/2 | OLED 目视 |
| TC-DIS-005 | 阈值设置→手动控制 | 阈值页任意子页 | 按 KEY1 | 进入手动控制页 | OLED 目视 |
| TC-DIS-006 | 手动控制→主页面 | 手动控制页 | 按 KEY1 | 返回主页面 P1 | OLED 目视 |
| TC-DIS-007 | 阈值编辑项选择 | 阈值页 | 按 KEY5 | 光标 `<<` 标识循环移动（7 项） | OLED 目视 |
| TC-DIS-008 | 阈值增加 | 阈值页，选中某项 | 按 KEY3 | 对应阈值增加（温度+1.0, PH+0.1, 水位+5%, 空气+10） | OLED 显示变化 |
| TC-DIS-009 | 阈值减少 | 阈值页，选中某项 | 按 KEY4 | 对应阈值减少（不小于 0） | OLED 显示变化 |
| TC-DIS-010 | 手动控制设备选择 | 手动控制页 | 按 KEY5 | 选中项显示 `>K3` 标识，循环 6 个设备 | OLED 目视 |
| TC-DIS-011 | 手动控制开关切换 | 手动控制页，选中某项 | 按 KEY3 | 选中设备 ON↔OFF 状态切换 | OLED 显示变化 |
| TC-DIS-012 | 汉字显示正确性 | 主页面 P1 | 观察汉字标签 | 温度、空气、水位、PH值 汉字清晰可辨 | OLED 目视 |
| TC-DIS-013 | 1s 自动刷新 | 无按键操作 | 等待 2s | 传感器数据自动更新（即使无按键） | 数值变化可见 |
| TC-DIS-014 | 页面指示器 | 任意页面 | 观察底部 | 右下角显示 `P1/2` 或 `P2/2` 或 `P1/1` | OLED 目视 |
| TC-DIS-015 | 混合字体布局 | 主页面 P1 | 观察每行 | 左侧 16px 汉字标签 + 右侧 6×12 ASCII 数值 | OLED 目视 |

### 2.6 按键扫描模块 (app_key)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-KEY-001 | KEY1 检测 | PA8 接按键 | 按下 KEY1 | `g_key_event = 1`，信号量释放 | 显示页面切换 |
| TC-KEY-002 | KEY2 检测 | PB3 接按键 | 按下 KEY2 | `g_key_event = 2` | 子页翻页 |
| TC-KEY-003 | KEY3 检测 | PB4 接按键 | 按下 KEY3 | `g_key_event = 3` | 阈值+1 或开关切换 |
| TC-KEY-004 | KEY4 检测 | PB8 接按键 | 按下 KEY4 | `g_key_event = 4` | 阈值-1 |
| TC-KEY-005 | KEY5 检测 | PB1 接按键 | 按下 KEY5 | `g_key_event = 5` | 编辑项切换 |
| TC-KEY-006 | 防抖验证 | 正常按按键 | 快速按一次 KEY1 | 只触发一次事件（3 次采样确认，150ms） | 页面只切换一次 |
| TC-KEY-007 | 长按不重复 | 按住 KEY1 不放 | 持续 2s | 只触发一次事件 | 页面只切换一次 |
| TC-KEY-008 | 多按键不冲突 | 任意状态 | 同时按下 KEY1+KEY3 | 优先扫描到的按键生效（KEY1>KEY3） | 按优先级响应 |

### 2.7 WiFi 通信模块 (app_wifi)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-WIF-001 | USART2 初始化 | PA2/PA3 接 ESP-01S | 上电 | USART2 配置为 115200 8N1，PA2=TX, PA3=RX | 示波器测空闲电平 |
| TC-WIF-002 | ESP-01S AT 指令 | ESP-01S 已接线 | 上电自动执行 | 发送 AT/CWMODE/CWJAP/CIPSERVER 指令序列 | 逻辑分析仪抓串口 |
| TC-WIF-003 | 传感器数据帧发送 | WiFi 已连接 | 等待 1s | USART2 发送 0xAA 0x01 帧，含温度/PH/水位/空气/模式/倒计时 | 逻辑分析仪 |
| TC-WIF-004 | 设备状态帧发送 | WiFi 已连接 | 等待 1s | USART2 发送 0xAA 0x02 帧，含继电器位图+报警+喂食状态 | 逻辑分析仪 |
| TC-WIF-005 | 帧校验和验证 | 发送任意帧 | 捕获帧数据 | 最后一个数据字节前为 SUM(CMD+LEN+DATA) & 0xFF | 手动计算验证 |
| TC-WIF-006 | 断线重连 | WiFi 连接后断开 | 等待 5s | `g_wifi_connected = 0`，每 5s 重试连接 | 观察连接状态 |
| TC-WIF-007 | SSID 配置 | 源码中硬编码 | 修改 `YOUR_SSID`/`YOUR_PASSWORD` | 实际连接到指定 WiFi 网络 | ESP-01S 串口响应 |

### 2.8 汉字字库模块 (font_lib)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-FNT-001 | Unicode 二分查找 | font_lib 已编译 | 调用 `font_get_bitmap(0x6E29)` (温) | 返回非 NULL 指针，指向 32 字节点阵 | 代码验证 |
| TC-FNT-002 | 不存在字符查找 | font_lib 已编译 | 调用 `font_get_bitmap(0x4F60)` (你) | 返回 NULL | 代码验证 |
| TC-FNT-003 | 字符存在性检查 | font_lib 已编译 | 调用 `font_char_exists(0x5EA6)` (度) | 返回 1 | 代码验证 |
| TC-FNT-004 | 32 字符完整性 | font_lib 已编译 | 遍历 unicode_table 全部 32 项 | 每项 `font_char_exists` 返回 1 | 代码验证 |
| TC-FNT-005 | 绘制汉字到 OLED | OLED 已初始化 | 调用 `oled_draw_chinese_char(0, 0, 0x6E29)` | OLED 左上角显示"温"字，占 pages 0-1 | OLED 目视 |
| TC-FNT-006 | UTF-8 字符串绘制 | OLED 已初始化 | 调用 `oled_draw_chinese_string(0, 0, "温度")` | 显示"温度"两字，间距 16px | OLED 目视 |
| TC-FNT-007 | 混合行绘制 | OLED 已初始化 | 调用 `oled_draw_mix_line(0, "温度", "24.0")` | 左侧"温度"+ 右侧"24.0" | OLED 目视 |
| TC-FNT-008 | 汉字点阵数据正确性 | font_lib 编译验证 | Python 脚本比对原始 bin_data | 32 字符点阵与原始数据完全一致 | 自动化脚本已验证 ✅ |

### 2.9 OLED 底层驱动 (drv_oled)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-OLD-001 | I2C GPIO 初始化 | PA11/PA12 接 OLED | 上电 | PA11=SDA, PA12=SCL 配置为推挽输出 50MHz | 万用表测高电平 |
| TC-OLD-002 | I2C 起始信号 | OLED 接线正确 | 调用 `oled_i2c_start()` | SDA 从高到低跳变时 SCL 为高 | 示波器 |
| TC-OLD-003 | I2C 停止信号 | I2C 已初始化 | 调用 `oled_i2c_stop()` | SDA 从低到高跳变时 SCL 为高 | 示波器 |
| TC-OLD-004 | OLED 初始化序列 | 上电 | 调用 `oled_init()` | 发送完整 SSD1306 初始化命令，屏幕亮起 | OLED 显示 |
| TC-OLD-005 | 清屏 | OLED 已初始化 | 调用 `oled_clear()` | 全屏变黑（无像素点亮） | OLED 目视 |
| TC-OLD-006 | ASCII 字符绘制 | OLED 已初始化 | 调用 `oled_draw_char(0, 0, 'A')` | 第 0 页显示 'A' | OLED 目视 |
| TC-OLD-007 | ASCII 字符串绘制 | OLED 已初始化 | 调用 `oled_draw_string(0, 0, "Hello")` | 第 0 页显示 "Hello" | OLED 目视 |
| TC-OLD-008 | 整数绘制 | OLED 已初始化 | 调用 `oled_draw_number(0, 0, -123)` | 显示 "-123" | OLED 目视 |
| TC-OLD-009 | 浮点数绘制 | OLED 已初始化 | 调用 `oled_draw_float(0, 0, 24.5, 1)` | 显示 "24.5" | OLED 目视 |
| TC-OLD-010 | 缓冲区刷新 | 修改缓冲区后 | 调用 `oled_refresh()` | 整屏内容更新为缓冲区数据 | OLED 目视 |
| TC-OLD-011 | 像素绘制 | OLED 已初始化 | 调用 `oled_draw_pixel(64, 32, 1)` | 屏幕中央点亮一个像素 | OLED 细看 |
| TC-OLD-012 | 越界保护 | OLED 已初始化 | 调用 `oled_draw_pixel(200, 100, 1)` | 无操作，不崩溃 | 代码无异常 |

### 2.10 共享数据与 IPC (app_data)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-DAT-001 | 数据初始化 | 上电 | 调用 `app_data_init()` | g_sensor/g_status/g_threshold 清零，阈值加载默认值 | 代码验证 |
| TC-DAT-002 | 默认阈值 | 初始化后 | 检查 g_threshold | temp_lower=20, temp_upper=30, air=500, ph_low=6.5, ph_high=8.5, wl_min=40, wl_max=80, feed=43200 | FinSH |
| TC-DAT-003 | 互斥量保护数据 | 多线程运行 | 观察 g_sensor 并发访问 | 无数据竞争，值始终一致 | 长时间运行无异常 |
| TC-DAT-004 | 互斥量保护 OLED | 多线程运行 | 观察 OLED 显示 | 无花屏、乱码 | OLED 目视 |
| TC-DAT-005 | 按键信号量 | 按下按键 | 观察 sem_key | 信号量计数 +1，显示线程成功接收 | 显示响应按键 |
| TC-DAT-006 | 报警邮箱 | 触发报警条件 | 观察 mb_alarm | 报警事件通过邮箱传递，t_alarm 收到并处理 | 蜂鸣器响应 |

### 2.11 系统集成 (main)

| 编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 验证方式 |
|------|--------|---------|---------|---------|---------|
| TC-SYS-001 | 完整启动流程 | 上电 | 等待 5s | 所有 7 个业务线程启动，OLED 显示主页面 | 系统正常运行 |
| TC-SYS-002 | 线程优先级验证 | 系统运行 | FinSH: `list_thread` | 所有线程状态正确，优先级与设计一致 | FinSH 命令 |
| TC-SYS-003 | 内存使用 | 系统运行 | FinSH: `list_thread` | 各线程栈使用量在分配范围内，无溢出 | FinSH 命令 |
| TC-SYS-004 | 长时间稳定性 | 系统运行 | 运行 24h | 无死机、无内存泄漏、数据持续更新 | 长跑测试 |
| TC-SYS-005 | 全功能联动 | 系统运行 | 水温过低 → 加热启动 → 报警 → OLED 更新 | 控制+报警+显示三模块同时响应且正确 | 综合观察 |
| TC-SYS-006 | 断电恢复 | 正常运行中 | 断电后重新上电 | 系统重启，加载默认阈值，恢复正常运行 | 上电观察 |

---

## 三、测试用例统计

| 模块 | 用例数 | 覆盖功能点 |
|------|--------|-----------|
| 传感器采集 | 7 | DS18B20、ADC 三通道、断线容错、采集周期 |
| 自动控制 | 10 | 温度/水位/空气控制、手动模式、模式切换 |
| 声光报警 | 8 | 四种报警源、报警解除、报警关闭、邮箱传递 |
| 喂食控制 | 7 | 定时/手动喂食、舵机 PWM、倒计时、状态标志 |
| OLED 显示 | 15 | 三页面、子页翻页、阈值编辑、手动控制、汉字显示、混合字体 |
| 按键扫描 | 8 | 五按键检测、防抖、长按、优先级 |
| WiFi 通信 | 7 | AT 指令、数据帧、校验和、断线重连 |
| 汉字字库 | 8 | 查找、绘制、UTF-8 解析、数据正确性 |
| OLED 底层 | 12 | I2C、SSD1306 初始化、绘图函数、越界保护 |
| 共享数据/IPC | 6 | 互斥量、信号量、邮箱、默认值 |
| 系统集成 | 6 | 启动流程、线程状态、长时间稳定性、联动 |
| **合计** | **94** | |

---

## 四、测试环境

| 项目 | 规格 |
|------|------|
| 主控 | STM32F103C8T6 |
| 操作系统 | RT-Thread v3.1.4 |
| 开发环境 | RT-Thread Studio |
| 调试工具 | FinSH MSH 终端 |
| 测量工具 | 示波器、万用表、逻辑分析仪 |
| 通信验证 | USB-TTL 适配器接 USART2 |

---

*文档结束。*

# 智能鱼缸系统代码问题总结与解决方案报告

**项目状态**：STM32F103C8T6 裸机项目  
**审查日期**：2026-05-04  
**审查范围**：全部核心文件（main.c、TimingSet.c、bsp_adc.*、GeneralTIM.*、bsp_Key.c 等）

---

## 一、严重问题（必须立即修复）

### 1. ADC 在中断中阻塞等待（最高优先级）
**问题描述**：
- `Timing_5ms()` 中仍存在 `while(!ADC_GetFlagStatus(ADC1, ADC_FLAG_EOC))`
- 在 TIM2 1ms 中断服务函数中执行阻塞操作，严重影响系统实时性

**影响**：
- 按键扫描、蜂鸣器驱动响应延迟
- 可能导致中断丢失或系统卡顿

**解决方案**：
```c
// 推荐方案：使用 DMA + 扫描模式（最佳）
void ADC_DMA_Init(void)
{
    // 配置 DMA + ADC ScanConvMode = ENABLE + Continuous
    ADC_RegularChannelConfig(ADC1, ADC_Channel_0, 1, ADC_SampleTime_55Cycles5);
    ADC_RegularChannelConfig(ADC1, ADC_Channel_1, 2, ADC_SampleTime_55Cycles5);
    ADC_RegularChannelConfig(ADC1, ADC_Channel_4, 3, ADC_SampleTime_55Cycles5);
    ADC_DMACmd(ADC1, ENABLE);
    ADC_ScanConvMode = ENABLE;
    ADC_Cmd(ADC1, ENABLE);
}

// Timing_5ms() 改为直接读取 DMA 缓冲区



2. TIM3 舵机 PWM 初始化不完整
问题描述：

GeneralTIM_3_Mode_Config() 只配置了时基，未配置 PWM 输出模式
main.c 中仅 TIM_Cmd(TIM3, ENABLE)，未设置 CCR 和 OC 模式

二、中等问题
3. 喂食逻辑存在 Bug
问题描述：

Timing_1s() 中 if (Record.feedCountdown > 30 - 3) 逻辑混乱
倒计时从 30 开始，打开时间计算不准确

4. 手动模式安全超时机制不完善
问题描述：

切换到自动模式时未强制关闭所有继电器
超时后仅蜂鸣器提示，未完全确保安全


三、轻微问题与优化建议

序号,问题,建议
1,bsp_adc.h/c 仍有历史残留代码,删除未使用的 DMA 函数和重复定义
2,OLED 显示中使用硬编码汉字,建议封装统一显示接口
3,bsp_esp01s.c 仍为 TODO,至少实现基本 AT 初始化和 WiFi 连接判断
4,未启用独立看门狗,"在 main() 中添加 IWDG_Init(4, 625); 并定期喂狗"
5,水位转换使用整数除法,改为 val * 100.0f / 4095.0f 提高精度
6,部分文件空或未使用（如 oled_IndexCtr.c）,删除或实现
7,代码风格不统一,增加统一注释格式，清理多余空行
# 智能鱼缸系统 — 业务逻辑流程图

> 版本：V3.1 | 日期：2026-05-05 | 状态：**裸机版**
> 关联文档：[需求规格书](./智能鱼缸系统需求规格书.md) | [代码框架](./智能鱼缸系统代码框架与业务流程.md) | [OLED显示规划](./OLED显示规划.md)

---

## 目录

1. [硬件引脚分配](#1-硬件引脚分配)
2. [系统启动流程](#2-系统启动流程)
3. [TIM2 中断调度时序](#3-tim2-中断调度时序)
4. [数据流总览](#4-数据流总览)
5. [传感器采集流程](#5-传感器采集流程)
6. [自动控制流程](#6-自动控制流程)
7. [报警逻辑流程](#7-报警逻辑流程)
8. [喂食控制流程](#8-喂食控制流程)
9. [按键业务流程](#9-按键业务流程)
10. [OLED 显示刷新流程](#10-oled-显示刷新流程)
11. [WiFi 通信流程](#11-wifi-通信流程)
12. [模块间交互总览](#12-模块间交互总览)
13. [源文件清单](#13-源文件清单)
14. [变更记录](#14-变更记录)

---

## 1. 硬件引脚分配

### 1.1 传感器与执行器引脚

| 类别 | 器件 | 引脚 | 接口类型 | 说明 |
|------|------|------|----------|------|
| **传感器** | DS18B20 温度 | PB13 | 1-Wire | 水温采集 |
| | 水位传感器 | PA1 | ADC1_IN1 | 水位百分比 |
| | PH 传感器 | PA0 | ADC1_IN0 | 酸碱度 |
| | MQ-135 空气质量 | PA4 | ADC1_IN4 | 空气质量原始值 |
| **显示** | OLED | PA11(SCL) / PA12(SDA) | I2C 软模拟 | 128×64 显示屏 |
| **执行器** | 加热继电器 | PB12 | GPIO 推挽 | 高电平有效 |
| | 加水继电器 | PB14 | GPIO 推挽 | 高电平有效 |
| | 排水继电器 | PB15 | GPIO 推挽 | 高电平有效 |
| | 增氧继电器 | PA15 | GPIO 推挽 | 高电平有效 |
| | 舵机 SG90 | PA6 | TIM3_CH1 PWM | 50Hz 喂食控制 |
| | 蜂鸣器+LED | PB9 | GPIO 推挽 | 报警指示 |
| **通信** | ESP-01S WiFi | PA2(TX) / PA3(RX) | USART2 | 远程数据上报 |
| **输入** | KEY1 | PA8 | 上拉输入 | 高电平触发 |
| | KEY2 | PB3 | 上拉输入 | 高电平触发 |
| | KEY3 | PB4 | 上拉输入 | 高电平触发 |
| | KEY4 | PB8 | 上拉输入 | 高电平触发 |
| | KEY5 | PB1 | 上拉输入 | 高电平触发 |

---

## 2. 系统启动流程

系统上电后，依次完成时钟配置、外设初始化、默认参数加载、启动画面显示，最后进入主循环等待中断调度。

```
┌─────────────────────────────────┐
│         上电 / 复位              │
└───────────────┬─────────────────┘
                ▼
┌─────────────────────────────────┐
│  时钟与中断基础配置              │
│  · 系统时钟 → 72MHz             │
│  · NVIC 中断优先级分组           │
│  · SysTick 1ms 节拍             │
└───────────────┬─────────────────┘
                ▼
┌─────────────────────────────────┐
│  外设初始化                      │
│  · OLED (PA11/PA12 I2C)         │
│  · DS18B20 (PB13 1-Wire)        │
│  · ADC (PA0/PA1/PA4 三通道)     │
│  · 4路继电器 (PB12/PB14/PB15/PA15)│
│  · 舵机 (PA6 TIM3 PWM)          │
│  · WiFi ESP-01S (PA2/PA3 USART2)│
│  · 按键 (PA8/PB3/PB4/PB8/PB1)  │
│  · 蜂鸣器+LED (PB9)             │
└───────────────┬─────────────────┘
                ▼
┌─────────────────────────────────┐
│  加载系统默认参数                │
│  · 运行模式 → 自动              │
│  · 报警使能 → 开启              │
│  · 喂食倒计时 → 设定默认间隔    │
│  · 各阈值参数 → 设定默认值      │
└───────────────┬─────────────────┘
                ▼
┌─────────────────────────────────┐
│  OLED 显示启动画面              │
│  · 显示 "智能鱼缸系统"          │
│  · 延时约 2 秒                  │
└───────────────┬─────────────────┘
                ▼
┌─────────────────────────────────┐
│  TIM2 定时器中断调度启动         │
│  · 配置 TIM2 为 1ms 中断周期    │
│  · 使能并启动 TIM2              │
└───────────────┬─────────────────┘
                ▼
┌─────────────────────────────────┐
│  进入主页面1（数据显示页）       │
└───────────────┬─────────────────┘
                ▼
┌─────────────────────────────────┐
│  while(1) 主循环                │
│  · 检查 WiFi 接收标志 → 处理    │
│  · 检查按键事件标志 → 处理      │
│  · 其他非周期性任务              │
└─────────────────────────────────┘
```

**要点说明**：
- 主循环只处理非周期性事件（WiFi 接收、按键响应）
- 所有周期性采集、控制、显示任务由 TIM2 中断调度完成
- 系统为裸机架构，无 RTOS，通过中断+标志位实现多任务

---

## 3. TIM2 中断调度时序

TIM2 以 1ms 为基本节拍，通过全局计数器取模运算，将任务分配到不同时间片执行。

### 3.1 调度时间轴

```
 ──┬────┬────┬────┬────┬────┬────┬────┬────┬────┬────┬────┬───→ 时间
   0    1    2    3    4    5   ...  10  ...   50  ...  500 ... 1000
   │    │    │    │    │    │        │        │        │       │
   ▼    ▼    ▼    ▼    ▼    ▼        ▼        ▼        ▼       ▼
  BEEP LED  ──   ──  ADC  ADC     滤波     按键    传感器   显示
  1ms  2ms       5ms            10ms     50ms   控制报警  喂食WiFi
```

### 3.2 各周期任务一览

| 周期 | 任务函数 | 具体功能 |
|------|----------|----------|
| 1ms | BEEP 状态机 | 蜂鸣器响/停节奏控制 |
| 2ms | LED 扫描 | LED 闪烁节奏控制 |
| 5ms | ADC 轮询 | PA0/PA1/PA4 三通道轮流采集 |
| 10ms | 数据滤波 | 传感器中间值处理与平滑 |
| 50ms | 按键扫描 | 5 路按键消抖与事件检测 |
| 500ms | 传感器+控制+报警 | 温度/水位/PH/空气采集 → 自动控制 → 报警检测 |
| 1s | 显示+喂食+WiFi | OLED 刷新 + 喂食状态机 + WiFi 数据上报 |

**调度逻辑**：每个中断到来时 `g_tick_ms++`，各周期任务通过 `g_tick_ms % N == 0` 判断是否执行。同一中断内任务按固定顺序串行执行，优先级：BEEP > LED > ADC > 滤波 > 按键 > 传感器/控制/报警 > 显示/喂食/WiFi。

---

## 4. 数据流总览

这是整个系统的数据流向总图，展示从传感器采集到显示、控制、报警的完整链路。

```
┌─────────────────────────────────────────────────────────────────┐
│                         传感器采集层                             │
│                                                                 │
│  DS18B20 ──1-Wire──→ 水温值 (°C)                               │
│  MQ-135  ───ADC───→ 空气质量 (0~4095)                          │
│  水位     ───ADC───→ 水位百分比 (0~100%)                        │
│  PH      ───ADC───→ PH 值 (0~14)                               │
└─────────────────────┬───────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                       全局数据变量                               │
│                                                                 │
│  waterTemp / waterLevel / phValue / airQuality                  │
│  tempLower / tempUpper / waterLevelMin / waterLevelMax          │
│  airQualityMax / phLower / phUpper                              │
│  runMode / feedCountdown / alarmEnable                          │
└───┬──────────┬──────────┬──────────┬──────────┬────────────────┘
    ▼          ▼          ▼          ▼          ▼
┌────────┐┌────────┐┌────────┐┌────────┐┌────────────┐
│ OLED   ││ 自动   ││ 报警   ││ WiFi   ││ 喂食       │
│ 显示   ││ 控制   ││ 检测   ││ 上报   ││ 控制       │
└────┬───┘└────┬───┘└────┬───┘└────┬───┘└─────┬──────┘
     │         │         │         │           │
     ▼         ▼         ▼         ▼           ▼
  128×64   继电器     蜂鸣器    ESP-01S     舵机SG90
  屏幕     开/关     LED闪烁    远程服务器   开/关3秒
```

**数据流向说明**：

1. **传感器 → 全局变量**：500ms 周期采集，结果写入全局变量
2. **全局变量 → OLED**：1s 周期读取并刷新屏幕
3. **全局变量 → 自动控制**：500ms 周期判断阈值，输出继电器控制信号
4. **全局变量 → 报警**：500ms 周期检测异常，触发蜂鸣器/LED
5. **全局变量 → WiFi**：1s 周期打包上报
6. **按键 → 全局变量**：用户操作修改页面/模式/阈值
7. **WiFi → 全局变量**：远程指令修改控制参数

---

## 5. 传感器采集流程

每 500ms 由 TIM2 中断触发一次完整采集，读取 4 路传感器数据并写入全局变量。

### 5.1 采集流程图

```
      Sensor_Read() 每500ms调用
              │
    ┌─────────┼─────────┬─────────────┐
    ▼         ▼         ▼             ▼
┌────────┐┌────────┐┌──────────┐┌──────────┐
│DS18B20 ││ MQ-135 ││ 水位     ││ PH       │
│PB13    ││ PA4    ││ PA1      ││ PA0      │
│1-Wire  ││ ADC_IN4││ ADC_IN1  ││ ADC_IN0  │
└───┬────┘└───┬────┘└────┬─────┘└────┬─────┘
    │         │          │           │
    ▼         ▼          ▼           ▼
 水温(°C)  空气质量   水位(%)     PH值
  float    uint16     uint8      float
    │         │          │           │
    ▼         ▼          ▼           ▼
┌─────────────────────────────────────────┐
│        全局数据变量 (统一存储)            │
│  waterTemp / airQuality / waterLevel    │
│  / phValue                              │
└─────────────────────────────────────────┘
```

### 5.2 各传感器采集说明

| 传感器 | 引脚 | 采集方式 | 数据转换 | 范围 |
|--------|------|----------|----------|------|
| DS18B20 | PB13 | 1-Wire 协议直接读取 | 芯片内部转换，直接输出 °C | -55 ~ +125°C |
| MQ-135 | PA4 (ADC1_IN4) | ADC 寄存器读取 | 原始 ADC 值 | 0 ~ 4095 |
| 水位 | PA1 (ADC1_IN1) | ADC 寄存器读取 | ADC × 100 / 4095 → 百分比 | 0 ~ 100% |
| PH | PA0 (ADC1_IN0) | ADC 寄存器读取 | ADC × 14.0 / 4095 → PH 值 | 0 ~ 14 |

**ADC 轮询机制**：TIM2 每 5ms 触发一次 ADC 轮询，三通道（PA0/PA1/PA4）轮流采集，10ms 周期进行中间值滤波平滑处理。500ms 周期时读取滤波后的稳定值。

---

## 6. 自动控制流程

每 500ms 由 TIM2 中断触发，系统读取传感器数据，与用户设定的阈值比较，自动控制 4 路继电器。仅在自动模式下生效。

### 6.1 控制总流程

```
       Auto_Control() 每500ms调用
                │
                ▼
        ┌───────────────┐
        │ 读取传感器数据  │
        │ waterTemp      │
        │ waterLevel     │
        │ airQuality     │
        └───────┬───────┘
                ▼
        ┌───────────────┐     否
        │ runMode == 自动 ├──────────→ 直接返回，不控制
        └───────┬───────┘
                │ 是
        ┌───────┴───────────────────────────┐
        ▼               ▼                   ▼
  ┌───────────┐  ┌─────────────┐  ┌──────────────┐
  │ 温度控制   │  │ 水位控制     │  │ 空气质量控制  │
  │ (滞环)    │  │ (区间判断)   │  │ (阈值开关)   │
  └─────┬─────┘  └──────┬──────┘  └──────┬───────┘
        │               │                │
        ▼               ▼                ▼
  relayHeat      relayDrain         relayOxygen
  ON / OFF       relayFill          ON / OFF
                 ON / OFF
```

### 6.2 温度控制（滞环控制）

温度控制采用滞环（迟滞）逻辑，防止继电器在阈值附近频繁切换。

```
  温度轴 ▲
         │
  tempUpper ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  relayHeat = OFF（停止加热）
         │
         │       ┌─────────────┐
         │       │  安全区间    │
         │       │  维持当前状态 │
         │       └─────────────┘
         │
  tempLower ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  relayHeat = ON（开始加热）
         │
         └──────────────────────────→ 温度值

  工作过程：
  ① 水温从高降低 → 低于 tempLower → 加热继电器开启
  ② 水温逐渐上升 → 高于 tempUpper → 加热继电器关闭
  ③ 在上下限之间 → 保持当前状态不变（滞环效果）
```

### 6.3 水位控制（区间判断）

水位控制根据当前水位百分比与设定的上下限比较，决定加水或排水。

```
  水位% ▲
        │
   100% ┼ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
        │
  waterLevelMax ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  relayDrain = ON（排水）
        │                              relayFill = OFF
        │
        │        ┌──────────────┐
        │        │   安全区间    │
        │        │ relayDrain=OFF│
        │        │ relayFill=OFF │
        │        └──────────────┘
        │
  waterLevelMin ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  relayFill = ON（加水）
        │                              relayDrain = OFF
        │
     0% ┼ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
        └──────────────────────────→ 水位值
```

### 6.4 空气质量控制（阈值开关）

空气质量控制逻辑简单，超过上限开启增氧，低于上限关闭。

```
  空气质量 ▲
           │
  airQualityMax ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  relayOxygen = ON（开始增氧）
           │
           │        ┌──────────────┐
           │        │  正常区间     │
           │        │ relayOxygen=OFF│
           │        └──────────────┘
           │
           └──────────────────────────→ 空气质量值

  逻辑：airQuality > airQualityMax → relayOxygen = ON
        airQuality ≤ airQualityMax → relayOxygen = OFF
```

### 6.5 继电器与引脚对应关系

| 控制对象 | 继电器引脚 | 控制条件 | 有效电平 |
|----------|-----------|----------|----------|
| 加热棒 | PB12 | waterTemp < tempLower → ON | 高电平 |
| 加水泵 | PB14 | waterLevel < waterLevelMin → ON | 高电平 |
| 排水泵 | PB15 | waterLevel > waterLevelMax → ON | 高电平 |
| 增氧泵 | PA15 | airQuality > airQualityMax → ON | 高电平 |

---

## 7. 报警逻辑流程

每 500ms 检测一次，当传感器数据超过安全阈值时，触发蜂鸣器和 LED 交替闪烁报警。

### 7.1 报警触发流程

```
       Alarm_Check() 每500ms调用
                │
                ▼
       ┌────────────────┐     否
       │ alarmEnable 开启 ├────────→ 直接返回
       └────────┬───────┘
                │ 是
                ▼
       ┌────────────────┐     否
       │ 存在报警事件？   ├────────→ 清除报警状态，蜂鸣器/LED 关闭
       └────────┬───────┘
                │ 是
                ▼
       ┌────────────────┐
       │ 进入报警状态    │
       │ · 蜂鸣器开启    │
       │ · LED 开启      │
       │ · 重置报警计时器 │
       └────────┬───────┘
                ▼
       ┌────────────────────────────────┐
       │         报警节奏控制             │
       │                                │
       │  0s         0.5s       3s      │
       │  ├────响────┤----停----┤→ 循环  │
       │  蜂鸣器ON   蜂鸣器OFF          │
       │  LED ON     LED OFF            │
       └────────────────────────────────┘
```

### 7.2 报警条件一览

报警事件由 Auto_Control() 在每次自动控制判断时同步检测，满足任一条件即标记报警事件：

| 报警条件 | 触发描述 |
|----------|----------|
| 温度过低 | waterTemp 低于安全温度下限 |
| 温度过高 | waterTemp 高于安全温度上限 |
| 水位过低 | waterLevel 低于最低安全水位 |
| 水位过高 | waterLevel 高于最高安全水位 |
| 空气质量超标 | airQuality 高于安全上限 |

**报警解除**：当 Auto_Control() 检测到所有参数回到安全区间，不再标记报警事件，蜂鸣器和 LED 自动静默。

---

## 8. 喂食控制流程

喂食可通过按键手动触发，也可通过倒计时自动触发。舵机打开 3 秒后自动关闭。

### 8.1 喂食状态机

```
┌──────────────┐         触发          ┌──────────────┐
│              │  (按键或倒计时归零)    │              │
│    待机状态   ├────────────────────→  │   喂食中     │
│              │                       │  舵机打开90°  │
│ · 倒计时递减  │                       │ · 计时器=0    │
│ · 等待触发    │                       │ · 计时器++    │
└──────────────┘                       └──────┬───────┘
      ▲                                       │
      │                                       │ 3秒后
      │         ┌──────────────┐              │
      │         │              │◄─────────────┘
      └─────────┤  舵机关闭    │
                │  重置倒计时   │
                │              │
                └──────────────┘
```

### 8.2 触发方式

```
  ┌──────────────────────────────┐
  │        喂食触发源             │
  └──────┬──────────────┬────────┘
         │              │
         ▼              ▼
  ┌──────────────┐ ┌───────────────┐
  │  手动触发     │ │  自动触发      │
  │              │ │               │
  │ KEY5 长按2秒 │ │ 倒计时归零     │
  │ (PB1)       │ │ 自动执行       │
  └──────┬───────┘ └───────┬───────┘
         │                 │
         └────────┬────────┘
                  ▼
         ┌──────────────┐
         │  设置触发标志  │
         └──────┬───────┘
                ▼
         Feed_Control() 每1s检查
```

### 8.3 时间参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 舵机打开角度 | 90° | 通过 TIM3_CH1 (PA6) PWM 控制 |
| 喂食持续时间 | 3 秒 | 舵机打开后自动关闭 |
| 默认倒计时间隔 | 用户设定 | 每次喂食完成后重置 |

---

## 9. 按键业务流程

5 个按键在不同页面下具有不同功能。按键扫描每 50ms 执行一次，采用连续 3 次一致确认的消抖策略。

### 9.1 按键扫描机制

```
      Key_Scan() 每50ms调用
              │
              ▼
     ┌────────────────┐
     │ 读取 5 路 GPIO  │
     │ KEY1~KEY5 电平  │
     └────────┬───────┘
              ▼
     ┌────────────────┐
     │ 消抖判断        │
     │ 连续3次一致？   │──── 否 ──→ 忽略，返回
     └────────┬───────┘
              │ 是
              ▼
     ┌────────────────┐
     │ 判断按键事件    │
     │ · 短按          │
     │ · 长按(≥2秒)    │
     └────────┬───────┘
              ▼
     ┌────────────────┐
     │ 写入全局事件变量│
     └────────┬───────┘
              ▼
     主循环检测到事件标志
     → Key_Event_Process() 处理
```

### 9.2 页面导航架构

系统共 3 个主页面，每个主页面包含若干子页面，通过 KEY1 和 KEY2 切换。

```
KEY1 切换主页面 (循环)
┌──────────┐    ┌──────────┐    ┌──────────┐
│ 主页面1   │───→│ 主页面2   │───→│ 主页面3   │──┐
│ (数据显示) │    │ (阈值设置) │    │ (手动控制) │  │
└──────────┘    └──────────┘    └──────────┘  │
     ▲                                         │
     └─────────────────────────────────────────┘

KEY2 切换子页面 (循环)
┌─────────────────────────────────────────────────────────┐
│ 主页面1                                                 │
│  ├── 子1-1: 水温 + PH + 状态栏(WiFi/模式/喂食)         │
│  ├── 子1-2: 水位 + 空气 + 加热/加水状态                 │
│  └── 子1-3: 排水/增氧 + ALM/模式                        │
├─────────────────────────────────────────────────────────┤
│ 主页面2                                                 │
│  ├── 子2-1: 温度 (上下限)                               │
│  ├── 子2-2: 空气 (上限)                                 │
│  ├── 子2-3: 水位 (上下限)                               │
│  └── 子2-4: PH (上下限)                                 │
├─────────────────────────────────────────────────────────┤
│ 主页面3                                                 │
│  ├── 子3-1: 加热                                        │
│  ├── 子3-2: 加水                                        │
│  ├── 子3-3: 排水                                        │
│  └── 子3-4: 增氧                                        │
└─────────────────────────────────────────────────────────┘
```

### 9.3 按键功能矩阵

| 按键 | 引脚 | 主页面1 (数据) | 主页面2 (阈值) | 主页面3 (手动) |
|------|------|---------------|---------------|---------------|
| **KEY1** | PA8 | → 切到主页面2 | → 切到主页面3 | → 切到主页面1 |
| **KEY2** | PB3 | 子页面循环切换 | 子页面循环切换 | 手动模式：子页面切换；自动模式：无效 |
| **KEY3** | PB4 | 无功能 | 选中值 +1 | 短按：自动↔手动切换；长按2s：同上 |
| **KEY4** | PB8 | 无功能 | 选中值 -1 | 手动模式：继电器 ON/OFF 切换 |
| **KEY5** | PB1 | 无功能 | 短按：上下限切换；长按2s：喂食 | 无功能 |

### 9.4 阈值设置流程

```
      进入主页面2（阈值设置）
              │
     ┌────────┴────────┐
     ▼                 ▼
  KEY2 切换         KEY5 切换
  参数类别          上限/下限
  温度→空气        (空气页只有
  →水位→PH         上限,KEY5无效)
     │                 │
     └────────┬────────┘
              ▼
     ┌────────────────┐
     │  当前选中字段    │
     │  用 "> " 光标   │
     │  标记显示       │
     └────────┬───────┘
              │
     ┌────────┴────────┐
     ▼                 ▼
  KEY3: +1          KEY4: -1
     │                 │
     └────────┬────────┘
              ▼
     ┌────────────────┐
     │  上下限互锁保护  │
     │  下限 ≤ 上限    │
     └────────────────┘
```

### 9.5 手动控制流程

```
      进入主页面3（手动控制）
              │
              ▼
     ┌────────────────┐
     │ runMode 是什么？ │
     └───┬─────────┬───┘
         │         │
    自动模式    手动模式
         │         │
         ▼         ▼
  ┌────────────┐ ┌────────────────────┐
  │ 显示       │ │ KEY2: 切换继电器   │
  │ "自动模式！"│ │ 加热→加水→排水→增氧│
  │            │ │                    │
  │ KEY2: 无效 │ │ KEY3: 切回自动模式  │
  │ KEY4: 无效 │ │ (同时关闭所有继电器)│
  │            │ │                    │
  │ KEY3: 切换 │ │ KEY4: 选中继电器   │
  │ →手动模式  │ │ ON / OFF 切换     │
  └────────────┘ └────────────────────┘
```

**模式切换安全机制**：从自动切到手动时，所有继电器自动归零（全部关闭），防止手动模式下出现意外的执行器动作。

---

## 10. OLED 显示刷新流程

每 1 秒刷新一次屏幕，根据当前主页面和子页面显示对应内容。

### 10.1 显示路由总流程

```
      Display_Refresh() 每1s调用
              │
              ▼
     ┌────────────────┐
     │ 读取 currentPage│
     └───┬────┬────┬───┘
         │    │    │
         ▼    ▼    ▼
      ┌────┐┌────┐┌────┐
      │页1 ││页2 ││页3 │
      │数据││阈值││手动│
      └─┬──┘└─┬──┘└─┬──┘
        │     │     │
        ▼     ▼     ▼
     子页面选择    子页面选择    子页面选择
     1-1/1-2/1-3  2-1~2-4      3-1~3-4
        │     │     │
        ▼     ▼     ▼
     ┌────────────────────┐
     │  OLED 清屏          │
     │  绘制对应页面内容    │
     │  右上角显示 "x/N"   │
     │  页码               │
     └────────────────────┘
```

### 10.2 各页面显示内容

| 主页面 | 子页面 | 显示内容 |
|--------|--------|----------|
| **页1 数据** | 1-1 | 水温(°C)、PH值、状态栏(WiFi/模式/喂食) |
| | 1-2 | 水位(%)、空气质量、加热/加水继电器状态 |
| | 1-3 | 排水/增氧继电器状态、报警状态、运行模式 |
| **页2 阈值** | 2-1 | 温度上限、温度下限（带">"光标） |
| | 2-2 | 空气质量上限（仅上限项） |
| | 2-3 | 水位上限、水位下限（带">"光标） |
| | 2-4 | PH上限、PH下限（带">"光标） |
| **页3 手动** | 3-1 | 加热继电器 ON/OFF 或"自动模式！" |
| | 3-2 | 加水继电器 ON/OFF 或"自动模式！" |
| | 3-3 | 排水继电器 ON/OFF 或"自动模式！" |
| | 3-4 | 增氧继电器 ON/OFF 或"自动模式！" |

---

## 11. WiFi 通信流程

ESP-01S 通过 USART2（PA2=TX, PA3=RX, 115200 波特率）与 STM32 通信。
工作在 **AP 模式**，ESP-01S 自建热点并启动 TCP 服务器，APP 直连热点后通过 TCP Socket 与 STM32 通信。

### 11.1 WiFi 连接初始化流程（`ESP01S_Init`）

系统上电后在 `main()` 中调用 `ESP01S_Init()`，按以下顺序配置 ESP-01S：

```
┌────────────────────────────────────┐
│  USART2 硬件初始化                  │
│  PA2=AF_PP(TX)  PA3=FLOATING(RX)   │
│  115200bps, 8N1, RXNE 中断使能     │
└──────────────┬─────────────────────┘
               ▼
┌────────────────────────────────────┐
│  AT                                │
│  检测 ESP-01S 是否存活             │
│  超时 2s, 失败则 delay 1s 再试一次  │
└──────────────┬─────────────────────┘
               ▼
┌────────────────────────────────────┐
│  AT+CWMODE=2                       │
│  设置 AP 模式（热点模式）           │
└──────────────┬─────────────────────┘
               ▼
┌────────────────────────────────────┐
│  AT+CWSAP="FishTank","12345678",1,3│
│  创建 WiFi 热点                     │
│  SSID=FishTank  密码=12345678      │
│  信道=1  加密=WPA2_PSK             │
└──────────────┬─────────────────────┘
               ▼
┌────────────────────────────────────┐
│  AT+CIPMUX=1                       │
│  启用多连接模式（支持最多5路TCP）    │
└──────────────┬─────────────────────┘
               ▼
┌────────────────────────────────────┐
│  AT+CIPSERVER=1,8080               │
│  启动 TCP 服务器，监听端口 8080     │
└──────────────┬─────────────────────┘
               ▼
┌────────────────────────────────────┐
│  AT+CIPSTO=120                     │
│  TCP 连接超时设为 120 秒           │
└──────────────┬─────────────────────┘
               ▼
        初始化完成，进入主循环
```

> **注意**：AT 命令发送后以 "OK" 作为成功判断，遇到 "ERROR"/"FAIL" 或超时则判定失败。
> 每条 AT 命令均有独立超时（1~3s），任一步失败不影响后续步骤继续执行。

### 11.2 APP 连接拓扑

```
  📱 手机 APP                      🔧 STM32
       │                              │
       │  WiFi 连接                   │
       ▼                              │
  ┌────────────┐  UART (115200)  ┌─────────┐
  │ ESP-01S    │◄────────────────│  主控    │
  │ TCP 服务器 │                 │         │
  │ 端口 8080  │────────────────►│  MCU    │
  └────────────┘                 └─────────┘
       ▲
       │ TCP Socket
       │
  📱 手机 APP（同热点）
```

**APP 连接步骤**：
1. 手机 WiFi 设置中搜索并连接热点 `FishTank`（密码 `12345678`）
2. 手机与 ESP-01S 处于同一局域网，ESP-01S 默认 IP 通常为 `192.168.4.1`
3. APP 中输入 `192.168.4.1:8080`，建立 TCP 连接
4. 连接成功后 APP 收到 `"CONNECT"` 提示，即可收发自定义协议帧

### 11.3 运行时数据处理流程

#### 11.3.1 数据接收流程（`ESP01S_Process`，主循环每 100ms 调用）

```
┌─────────────────────┐
│  USART2 RX 中断      │
│  (PA3 接收字节)      │
└─────────┬───────────┘
          ▼
┌─────────────────────┐
│  写入环形缓冲区      │
│  s_ringBuf[512]     │
│  (ISR 中写入, 主循环读取) │
└─────────┬───────────┘
          ▼
┌─────────────────────┐
│  主循环调用          │
│  ESP01S_Process()   │
└─────────┬───────────┘
          ▼
┌─────────────────────┐
│  监测 WiFi 事件      │
│  · "WIFI CONNECTED" → 设在线  │
│  · "WIFI DISCONNECTED" → 设离线 │
│  · ",CONNECT" → 客户端已连接  │
│  · ",CLOSED"  → 客户端已断开  │
└─────────┬───────────┘
          ▼
┌─────────────────────┐
│  查找 +IPD 数据头    │
│  格式: +IPD,<id>,<len>:<data> │
└─────────┬───────────┘
          ▼
┌─────────────────────┐
│  提取协议帧          │
│  0xAA|CMD|LEN|DATA|SUM|0x55 │
└─────────┬───────────┘
          ▼
┌─────────────────────┐
│  命令分发 (ESP01S_HandleFrame) │
│  0x10 查询状态 → 回传传感器+设备状态 │
│  0x11 切换模式 → 更新 runMode │
│  0x12 继电器控制 → 继电器互斥安全检查 │
│  0x13 同步阈值 → 更新温度/PH/水质阈值 │
│  0x14 触发喂食 → Flag.feeding=1 │
│  0x15 报警开关 → 更新 alarmEnable │
│  0x16 喂食间隔 → 更新 feedInterval │
└─────────────────────┘
```

#### 11.3.2 数据上报流程（主循环每 ~1s 触发）

```
  ┌──────────────────┐
  │ 主循环定时器      │
  │ (100ms×10=1s)   │
  └────────┬─────────┘
           ▼
  ┌──────────────────┐
  │ 检查客户端连接    │
  │ s_clientConnected │
  │   == 0? 跳过     │
  └────────┬─────────┘
           ▼
  ┌──────────────────┐
  │ ESP01S_SendSensorData │
  │ 组装 14 字节负载  │
  │ · waterTemp (float LE)  │
  │ · phValue  (float LE)   │
  │ · waterLevel (uint8)    │
  │ · airQuality (uint16 BE) │
  │ · runMode (uint8)       │
  │ · feedCountdown (uint16 BE) │
  └────────┬─────────┘
           ▼
  ┌──────────────────┐
  │ AT+CIPSEND=<id>,<len> │
  │ → 等待 ">" 提示   │
  └────────┬─────────┘
           ▼
  ┌──────────────────┐
  │ 发送协议帧        │
  │ 0xAA|0x01|14|DATA|SUM|0x55 │
  │ → 等待 "SEND OK"  │
  └──────────────────┘
```

### 11.4 通信引脚

| 功能 | 引脚 | 接口 | 说明 |
|------|------|------|------|
| TX (发送) | PA2 | USART2 | STM32 → ESP-01S |
| RX (接收) | PA3 | USART2 | ESP-01S → STM32 |
| 波特率 | — | 115200 | 8N1, 无硬件流控 |

### 11.5 WiFi 连接失败对其他功能的影响

**结论：WiFi 连接失败或断开不会影响核心本地功能，仅 APP 远程控制不可用。**

| 功能模块 | 依赖 WiFi | 说明 |
|---------|----------|------|
| 温度采集 (DS18B20) | ❌ | 独立驱动，主循环直接读取 |
| 水位/PH/空气质量 (ADC) | ❌ | 独立 ADC 轮询 |
| 继电器控制 | ❌ | 本地 GPIO 直接驱动 |
| OLED 显示 | ❌ | 本地 I2C 软模拟驱动 |
| 按键操作 | ❌ | 本地 GPIO 扫描 |
| 喂食倒计时 | ❌ | 本地定时逻辑 |
| 蜂鸣器报警 | ❌ | 本地 GPIO 驱动 |
| **APP 远程控制** | ✅ | 唯一依赖 WiFi 的功能 |
| **传感器数据上报 APP** | ✅ | `s_clientConnected==0` 时跳过发送 |

**代码层面的防护机制**：

1. **初始化失败不阻塞系统**：`ESP01S_Init()` 中每条 AT 命令有独立超时，失败仅打日志，不 halt，不影响后续外设（DS18B20/ADC/OLED/继电器）的初始化
2. **发送前守卫检查**：`ESP01S_SendFrame()` 和所有 TX 函数开头均检查 `if (!s_clientConnected) return;`，无客户端连接时立即返回
3. **主循环非阻塞**：`ESP01S_Process()` 仅扫描缓冲区（无数据则瞬间返回），`ESP01S_SendSensorData()` 在无连接时立即跳过，不会卡死主循环
4. **串口中断优先级最低**：USART2 中断优先级为 3（Group 4），低于 TIM2 定时器（优先级 2），不影响传感器采集时序

---

## 12. 模块间交互总览

以下是完整的模块交互示意图，展示传感器、数据处理、显示、控制、报警、通信六大模块之间的数据流向。

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│   ┌───────────┐   500ms    ┌───────────────────┐                    │
│   │  传感器    │──────────→│                   │                    │
│   │  采集模块  │  原始数据  │   全局数据变量     │                    │
│   │           │            │                   │                    │
│   │ DS18B20   │            │ waterTemp         │                    │
│   │ MQ-135    │            │ waterLevel        │                    │
│   │ 水位      │            │ phValue           │                    │
│   │ PH        │            │ airQuality        │                    │
│   └───────────┘            │ runMode           │                    │
│                            │ 阈值参数           │                    │
│                            │ 继电器状态         │                    │
│                            └──┬───┬───┬───┬───┬┘                    │
│                               │   │   │   │   │                     │
│          ┌────────────────────┘   │   │   │   └───────────┐         │
│          │         ┌──────────────┘   │   └────────┐      │         │
│          │         │         ┌────────┘            │      │         │
│          ▼         ▼         ▼                     ▼      ▼         │
│   ┌───────────┐┌───────────┐┌───────────┐ ┌───────────┐┌─────────┐ │
│   │  OLED     ││ 自动控制  ││ 报警检测  │ │ WiFi 上报 ││ 喂食    │ │
│   │  显示     ││           ││           │ │           ││ 控制    │ │
│   │ (1s)     ││ (500ms)  ││ (500ms)  │ │ (1s)     ││ (1s)   │ │
│   └─────┬─────┘└─────┬─────┘└─────┬─────┘ └─────┬─────┘└────┬────┘ │
│         │            │            │              │           │      │
│         ▼            ▼            ▼              ▼           ▼      │
│   ┌──────────┐┌──────────┐┌──────────┐   ┌──────────┐┌──────────┐  │
│   │ 128×64   ││ 4路继电器││ 蜂鸣器   │   │ ESP-01S  ││ 舵机SG90 │  │
│   │ OLED屏   ││ ON/OFF   ││ LED闪烁  │   │ 远程通信 ││ PA6 PWM  │  │
│   │ PA11/12  ││ PB12/14  ││ PB9      │   │ PA2/3    ││ 3秒开关  │  │
│   └──────────┘│ PB15/PA15│└──────────┘   └──────────┘└──────────┘  │
│               └──────────┘                                         │
│                                                                     │
│   ┌───────────────────────────────────────────────────────────┐     │
│   │                      按键输入模块                          │     │
│   │                                                           │     │
│   │  KEY1~KEY5  ──50ms扫描──→  事件检测  ──→  主循环处理       │     │
│   │  PA8/PB3/   消抖判断       短按/长按      · 页面切换       │     │
│   │  PB4/PB8/                              · 模式切换        │     │
│   │  PB1                                  · 阈值调整        │     │
│   │                                       · 继电器手动控制   │     │
│   │                                       · 喂食触发        │     │
│   └───────────────────────────────────────────────────────────┘     │
│          │                                                           │
│          │  修改全局变量                                             │
│          ▼                                                           │
│     全局数据变量  ←── 也可被 WiFi 远程指令修改                        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 关键交互链路说明

| 链路 | 数据流 | 触发周期 | 说明 |
|------|--------|----------|------|
| 传感器 → 全局变量 | 原始数据 → 工程值 | 500ms | 4 路传感器采集并转换 |
| 全局变量 → OLED | 当前值 → 屏幕刷新 | 1s | 读取并绘制到屏幕 |
| 全局变量 → 自动控制 | 当前值 vs 阈值 → 继电器 | 500ms | 滞环/区间判断 |
| 全局变量 → 报警 | 超限检测 → 蜂鸣器 | 500ms | 异常时触发声光报警 |
| 全局变量 → WiFi | 打包数据 → 上报 | 1s | 远程监控 |
| 按键 → 全局变量 | 用户操作 → 参数修改 | 50ms 扫描 | 页面/模式/阈值修改 |
| WiFi → 全局变量 | 远程指令 → 参数修改 | 事件驱动 | 远程控制 |
| 自动控制 → 报警 | 超限标记 → 报警事件 | 500ms | 控制与报警联动 |

---

## 13. 源文件清单

| 文件 | 路径 | 功能 |
|------|------|------|
| main.c | MDK_User/ | 系统入口，主循环 |
| oled_show.c | MDK_Hardware/OLED/ | OLED 显示（3主页面×子页面） |
| oled_show.h | MDK_Hardware/OLED/ | 显示函数声明 |
| bsp_Key.c | MDK_Hardware/ | 按键驱动+事件处理 |
| bsp_Key.h | MDK_Hardware/ | 按键引脚定义+事件枚举 |
| MyTypedef.h | MDK_APP/ | 全局数据结构 |
| MyTypedef.c | MDK_APP/ | 全局变量定义 |
| TimingSet.c | MDK_APP/ | 定时中断调度（1ms~1s） |
| TimingSet.h | MDK_APP/ | 定时函数声明 |
| GeneralTIM.c | MDK_APP/ | TIM2 定时器配置 |
| ds18b20.c | MDK_Hardware/ | DS18B20 温度驱动 |
| ds18b20.h | MDK_Hardware/ | DS18B20 引脚定义 |
| bsp_esp01s.c | MDK_Hardware/ | ESP-01S WiFi 驱动 |
| bsp_esp01s.h | MDK_Hardware/ | WiFi 函数声明 |
| oledChinese_6X12.h | MDK_Hardware/OLED/ | 32个汉字字库 |
| oled_drive.c/h | MDK_Hardware/OLED/ | OLED I2C 驱动 |
| oled_ShowDrive.c/h | MDK_Hardware/OLED/ | OLED 显示底层 |
| stm32f10x_it.c | MDK_User/ | TIM2 中断处理 |

---

## 14. 变更记录

| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|----------|
| V1.0 | 2025-05-01 | - | 初版，基础业务流程框架 |
| V2.0 | 2026-05-04 | - | 增加完整 ASCII 流程图，补充喂食/报警/手动控制流程 |
| V3.0 | 2026-05-04 | - | 全面重构：移除 C 代码和 struct 定义，改用通俗文字+ASCII 流程图；修正硬件引脚（DS18B20→PB13，KEY5→PB1，OLED→PA11/PA12）；增加模块间交互总览示意图；增加数据流总览章节；增加变更记录表格 |
| V3.1 | 2026-05-05 | - | 第 11 节 WiFi 通信流程重写：新增 11.1 初始化流程（AT 命令序列）、11.2 APP 连接拓扑与步骤、11.3.1 数据接收流程（新增 +IPD 解析与命令分发表）、11.3.2 数据上报流程（更正为主循环定时触发，含帧结构）、11.4 通信引脚表、11.5 WiFi 失败影响分析与防护机制 |

---

*文档结束。裸机版 V3.1 — 3主页面×子页面 OLED 架构，5键导航，TIM2 中断调度，WiFi 连接流程与失败影响分析已补全。*
